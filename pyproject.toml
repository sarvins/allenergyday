[project]
name = "allenergyday-server"
version = "0.1.0"
description = "Self-hosted SQLite + SSE backend for AllEnergyDay"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
]

[tool.uv]
package = false
